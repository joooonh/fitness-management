<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.mapper.FitnessVisitantMapper" >

	<select id="getAllFitnessVisitantsByUserNo" parameterType="int" resultType="FitnessVisitant">
		select 
			consulting_no					as no,
			consulting_reservation_date		as reservationDate,
			consulting_reservation_time		as reservationTime,
			consulting_purpose				as purpose,
			consulting_content				as content,
			consulting_status				as status,
			consulting_created_date 		as createdDate,
			consulting_updated_date			as updatedDate,
			user_no							as userNo,
			program_no						as programNo,
			employee_id						as empId
		from
			fitness_visitant
		where
			user_no = #{value}
	</select>

</mapper>